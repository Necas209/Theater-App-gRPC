syntax = "proto3";

service AuthManager {
  rpc Login (LoginRequest) returns (LoginReply);
}

// Login message
message LoginRequest {
  string user_name = 1;
  string password_hash = 2;
}

message LoginReply {
  bool login_status = 1;
  int32 user_id = 2;
  int32 user_type = 3;
}